apiVersion: v1
kind: Service
metadata:
    name: cache-service
    labels:
        app: cache
spec:
    type: ClusterIP
    selector:
        app: cache
    ports:
        - name: http
          protocol: TCP
          port: 80
          targetPort: 8000

apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
    name: cache-metrics
    labels:
        app: cache
        release: prometheus
spec:
    selector:
        matchLabels:
            app: cache
    endpoints:
        - port: http
          path: /metrics
          interval: 30s
